tags:
  - Auth
summary: Refresh access token
operationId: refreshUserSession
description: Refresh access token using a valid refresh token
security: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/refreshToken.yaml'
responses:
  '200':
    $ref: '../../components/responses/200-refresh.yaml'
